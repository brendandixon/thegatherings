%section
  .row
    .small-12.columns
      %h2= I18n.t(:title, scope: [:gatherings, :edit], name: @gathering.name)
      = render partial: 'shared/spacer'
      = render partial: 'form', locals: {url: gathering_path(@gathering)}
